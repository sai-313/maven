jenkins installation in linux

     1.sudo su -
     2.yum update -y
     3.sudo amazon-linux-extras install java-openjdk11
     4.wget https://get.jenkins.io/war-stable/2.387.1/jenkins.war
     5.java -jar jenkins.war --httpPort=80


jenkins installation in ubuntu

      1.sudo su -
      2.sudo apt-get update
      3.sudo apt install -y openjdk-11-jdk
      4.wget https://get.jenkins.io/war-stable/2.387.3/jenkins.war
      5.java -jar jenkins.war --httpPort=80


jenkins installation in linux

     sudo su -
     yum update -y
     sudo amazon-linux-extras install java-openjdk11
     yum install git maven -y
     wget https://get.jenkins.io/war-stable/2.387.1/jenkins.war
     java -jar jenkins.war --httpPort=80

Again we connect the same EC2 instance then we can execute the below commands

   sudo su -
   wget https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.75/bin/apache-tomcat-9.0.75.tar.gz
   tar xvzf apache-tomcat-9.0.75.tar.gz
   ls
   cd apache-tomcat-9.0.75/
     ls
     cd conf/
     ls
     vim server.xml ====>here we can change the port number 8080 to 80.
     vim tomcat-users.xml 
     cd ..
     ls
     cd apache-tomcat-9.0.75/
    ls
     cd webapps/
    ls
    cd host-manager/
     ls
     cd META-INF/
    ls
    vi context.xml 
     pwd
    cd ..
    ls
     cd manager/
    ls
    cd META-INF/
    ls
    vi context.xml 
     cd ../../..
     ls
     cd bin/
    ls -la
    ./startup.sh 
    history